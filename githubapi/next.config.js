/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["cdn.dummyjson.com", "avatars.githubusercontent.com"],
    },
  };
  
  module.exports = nextConfig;
  